<h1>ショッピングカート</h1>
<%= link_to "空にする", all_destroy_path, method: :delete, "data-confirm" => "本当に空にしますか？",  class:"btn-sm btn-danger" %>
  <table class="table">
    <tbody>
      <tr>
        <th>商品名</th>
        <th>単価（税込）</th>
        <th>数量</th>
        <th>小計</th>
        <th></th>
      </tr>
      <% @sum = 0 %>
      <% @cart_items.each do |cart_item| %>
      <tr>
        <td><%= image_tag cart_item.item.item_image_id.url %>
            <%= cart_item.item.name %></td>
        <td><%= cart_item.item.without_tax_price  %></td>
        <td>
        <%= form_for(cart_item) do |f| %>
          <%= f.number_field :item_number, min: 1, max: 10, step: 1 %>
          <%= f.submit "変更" %>
        <% end %>
        </td>
        <td><%= cart_item.item_number %></td>
        <%# 商品別の小計を商品価格×個数×消費税(1.1)で計算した後.floorメソッドで小数点以下を切り捨て %>
        <td><%= (cart_item.item.without_tax_price * cart_item.item_number * 1.1).floor%></td>
        <%# 合計金額の計算式を定義 %>
        <% @sum += cart_item.item.without_tax_price * cart_item.item_number * 1.1 %>
        <td>
          <%= link_to "削除する", cart_item_path(cart_item.id), method: :delete, class:"btn-sm btn-danger" %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <table class="table">
  <%# 上記で定義した合計金額の呼び出し %>
  <tr>合計金額
    <%= @sum.floor %>
  </tr>
  </table>

  <%= link_to "買い物を続ける", items_path  %>

  <%= link_to "情報入力にすすむ", new_delivery_address_path %>


       