

    <table class="table">
  		<thead>
   			<tr>
   				<th>ジャンル検索</th>
   			</tr>
   		</thead>
   		<tbody>
   			<% @genres.each do |genre| %>
   			<tr>
   				<td>
   					<%= genre.name %>
   				</td>
   			</tr>
        <% end %>
   		</tbody>
    </table>
      <%= @item.item_image_id %>
      <%= @item.name %>
      <%= @item.description %>
      ¥<%= tax.floor %>(税込)   <%# 税込み価格を表示。floorで小数点以下を切り捨て %>

      <%= form_for (item_number) do |from| %>
        <%= form_numbr_field :item_number, class:"",min: 1, max: 10 %>   <%# 個数選択のファーム（1〜10まで選べる) %>
        <%= f.submit "カートに入れる", class:"" %>
      <% end %>
